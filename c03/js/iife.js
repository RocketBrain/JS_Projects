// The below function is  immediately invoked. The variable does not store the
//function for later use. Instead, the variable holds the answer returned by the funcion.

var area = (function() {

var width = 3;
var height = 2;
return width*height;
}());


